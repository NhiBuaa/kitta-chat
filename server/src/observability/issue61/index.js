const {
  createIssue61AggregateMeasurementModule,
  CALL_PHASE,
  CALL_STAGE,
  CALL_OUTCOME,
  CALL_STAGE_MANIFEST,
  AUTHORIZED_HARD_CEILING_PER_PROCESS,
  MANIFEST_MAX_EXPORTED_SERIES_PER_PROCESS,
  calculateManifestMaxExportedSeriesPerProcess,
} = require("./issue61AggregateMeasurementModule");

module.exports = {
  createIssue61AggregateMeasurementModule,
  CALL_PHASE,
  CALL_STAGE,
  CALL_OUTCOME,
  CALL_STAGE_MANIFEST,
  AUTHORIZED_HARD_CEILING_PER_PROCESS,
  MANIFEST_MAX_EXPORTED_SERIES_PER_PROCESS,
  calculateManifestMaxExportedSeriesPerProcess,
};
