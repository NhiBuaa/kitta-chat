# Sidebar API Contract

The unified sidebar returns direct and group conversations through one cursor-based endpoint.

- Stable legacy conversation identifier
- Independent cursors per filter
- Batch-enriched targets and senders
- Unread and pinned state per participant
